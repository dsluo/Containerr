{
  "configurations": [
    {
      "type": "node-terminal",
      "name": "start:dev",
      "request": "launch",
      "command": "npm run start:dev",
      "cwd": "${workspaceFolder}"
    },
    {
      "type": "node-terminal",
      "name": "test",
      "request": "launch",
      "command": "npm test",
      "cwd": "${workspaceFolder}"
    }
  ]
}